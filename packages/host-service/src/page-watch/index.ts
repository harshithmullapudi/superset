export {
	HEARTBEAT_INTERVAL_MS,
	IDLE_TTL_MS,
	MAX_CONSECUTIVE_FAILURES,
	MAX_WATCHERS,
	type PageWatchApi,
	type PageWatchDeps,
	PageWatchManager,
	TICK_INTERVAL_MS,
} from "./page-watch-manager.ts";
export type {
	PageWatchAssignment,
	PageWatchStatus,
	WatchedThread,
} from "./types.ts";
